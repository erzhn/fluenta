# FLUENTA — 3 NEW FEATURES
# НЕ ЗАПРАШИВАЙ РАЗРЕШЕНИЯ. Все изменения автоматически.
# Запуск: $env:CLAUDE_CODE_MAX_OUTPUT_TOKENS=100000; claude --dangerously-skip-permissions < features4-prompt.txt
#
# Стек: Next.js 14 App Router, TypeScript, Tailwind CSS v4, Supabase, Groq API (llama-3.1-8b-instant)
# AI chat: POST /api/ai/chat → { messages, mode } → { reply }
# Auth: supabase JWT в Authorization header
# Profiles table: id, name, email, current_level, goal_type (уже есть), streak, xp, ...

## ══════════════════════════════════════════════════════════════
## ФИЧА 1: ONBOARDING FLOW ДЛЯ НОВЫХ ПОЛЬЗОВАТЕЛЕЙ
## ══════════════════════════════════════════════════════════════

### Что нужно сделать:

**1.1. Добавить поле `onboarding_completed` в profiles**

Создай файл `supabase/migrations/onboarding.sql`:
```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS onboarding_completed boolean DEFAULT false;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS learning_goal text DEFAULT 'general';
-- learning_goal values: 'tourist' | 'business' | 'exam' | 'general'
```

**1.2. Создать компонент `components/onboarding/OnboardingWizard.tsx`**

Многошаговый wizard с 3 шагами:

**Шаг 1 — Выбор цели** (goal selection):
```
Заголовок: "Какова твоя цель в английском?"
Карточки (с иконками и описаниями):
- 🌍 "Туристический English" (tourist)
  Описание: "Путешествия, отели, аэропорты, разговоры с иностранцами"
- 💼 "Бизнес английский" (business)
  Описание: "Переговоры, переписка, презентации, деловые встречи"
- 📝 "Подготовка к экзамену" (exam)
  Описание: "IELTS, TOEFL, Cambridge — системная подготовка"
- 💬 "Общий английский" (general)
  Описание: "Разностороннее развитие всех навыков"
```

**Шаг 2 — Текущий уровень** (level selection):
```
Заголовок: "Как ты оцениваешь свой уровень?"
Карточки:
- 🌱 Начинающий (A1-A2) — "Знаю базовые слова"
- 🌿 Средний (B1-B2) — "Могу объясниться"
- 🌳 Продвинутый (C1) — "Уверенно общаюсь"
```

**Шаг 3 — Рекомендованный план**:
Показать персонализированный план на основе goal + level:
- Список из 5-6 рекомендованных разделов (modules) для начала
- Кнопка "Начать обучение" → сохранить в Supabase и редирект на /dashboard

Логика рекомендаций:
```typescript
const RECOMMENDATIONS = {
  tourist: {
    title: "Туристический маршрут",
    description: "Фокус на разговорных навыках и практических ситуациях",
    modules: ["Vocabulary", "Mini Stories", "Listening", "Speaking Practice", "AI Tutor"],
    dailyGoal: 20,
  },
  business: {
    title: "Бизнес трек",
    description: "Профессиональный английский для карьерного роста",
    modules: ["Grammar", "Writing", "Vocabulary", "Reading Speed", "AI Tutor"],
    dailyGoal: 30,
  },
  exam: {
    title: "Экзаменационный трек",
    description: "Системная подготовка к международным экзаменам",
    modules: ["Grammar", "Reading Speed", "Listening", "Writing", "Word Formation", "Pronunciation"],
    dailyGoal: 45,
  },
  general: {
    title: "Универсальный трек",
    description: "Гармоничное развитие всех навыков",
    modules: ["AI Tutor", "Vocabulary", "Grammar", "Listening", "Mini Stories"],
    dailyGoal: 25,
  },
}
```

**Дизайн wizard:**
- Полноэкранный overlay поверх dashboard (z-50)
- Прогресс-бар вверху (шаг 1/3, 2/3, 3/3)
- Плавные анимации между шагами (framer-motion, AnimatePresence, slideX)
- Тёмный фон (#0F172A), карточки с hover-эффектами
- Кнопка "Далее" активна только после выбора

**1.3. Показывать wizard для новых пользователей**

В `app/(dashboard)/layout.tsx` (или создай новый если нет):
- После загрузки профиля: если `profile.onboarding_completed === false` → показать `<OnboardingWizard />`
- Wizard рендерится поверх всего контента через portal или условный рендер

Если `app/(dashboard)/layout.tsx` не существует, добавь логику в `app/(dashboard)/page.tsx` (dashboard главная страница).

Проверь как выглядит `app/(dashboard)/layout.tsx` и добавь туда:
```tsx
// После получения профиля:
const [showOnboarding, setShowOnboarding] = useState(false)
useEffect(() => {
  // Показать onboarding только для новых пользователей
  if (profile && !profile.onboarding_completed) {
    setShowOnboarding(true)
  }
}, [profile])

// В JSX добавить:
{showOnboarding && (
  <OnboardingWizard onComplete={() => setShowOnboarding(false)} />
)}
```

**1.4. Сохранение в Supabase при завершении wizard:**
```typescript
await supabase.from('profiles').update({
  learning_goal: selectedGoal,
  current_level: selectedLevel,
  onboarding_completed: true,
}).eq('id', userId)
```

---

## ══════════════════════════════════════════════════════════════
## ФИЧА 2: ГОЛОСОВОЙ AI ТЬЮТОР — ГОЛОСОВЫЕ ОТВЕТЫ
## ══════════════════════════════════════════════════════════════

Файлы для изменения:
- `app/(dashboard)/ai-tutor/page.tsx` (основной файл, 447 строк)
- `components/ai-tutor/ChatMessage.tsx`

### Что нужно сделать:

**2.1. Добавить "Voice Mode" в AI Tutor**

В хедере страницы ai-tutor добавить тогл "🎙 Voice Mode" (рядом с кнопками reset/mode).

Состояние: `const [voiceMode, setVoiceMode] = useState(false)`

**2.2. Изменить поведение микрофона в voice mode**

Сейчас: SpeechRecognition → transcript → вставляет в input → пользователь отправляет

Новое поведение в voice mode:
1. Пользователь нажимает 🎙 → начинается запись через MediaRecorder (захват аудио)
2. ОДНОВРЕМЕННО работает SpeechRecognition для получения текста транскрипции
3. Когда запись останавливается:
   - Создать blob URL из аудио (`URL.createObjectURL(audioBlob)`)
   - Добавить в messages сообщение с type: 'voice' + audioUrl + транскрипция (скрытая, только для API)
   - Отправить транскрипцию в AI API (как обычно)
4. Когда AI отвечает → автоматически прочитать ответ вслух через Web Speech Synthesis

**2.3. Обновить тип Message**

В `types/index.ts` или локально в файле добавить:
```typescript
interface Message {
  id: string
  role: 'user' | 'assistant'
  content: string
  timestamp: string
  // Новые поля:
  audioUrl?: string     // blob URL для воспроизведения голосового сообщения
  isVoice?: boolean     // было ли сообщение голосовым
}
```

**2.4. Обновить `ChatMessage.tsx`**

Если сообщение имеет `isVoice: true` и `audioUrl` → показать аудио-плеер вместо (или рядом с) текстом:

```tsx
// Для voice сообщений пользователя:
{message.isVoice && message.audioUrl ? (
  <div className="flex flex-col gap-2">
    {/* Аудио плеер */}
    <div className="flex items-center gap-2 bg-white/10 rounded-xl px-3 py-2">
      <span className="text-lg">🎙</span>
      <audio
        src={message.audioUrl}
        controls
        className="h-8 w-full max-w-[200px]"
        style={{ filter: 'invert(1)' }} // чтобы controls были светлыми
      />
    </div>
    {/* Транскрипция мелким шрифтом (опционально) */}
    {message.content && (
      <p className="text-xs text-white/40 italic">"{message.content}"</p>
    )}
  </div>
) : (
  // Обычный текст
  <p>{message.content}</p>
)}
```

**2.5. TTS — озвучивание ответов AI**

После получения ответа от AI, если `voiceMode === true`:
```typescript
const speakText = (text: string) => {
  // Убрать markdown символы и эмодзи для чистого произношения
  const clean = text
    .replace(/[*_`#]/g, '')
    .replace(/🇷🇺:.*$/gm, '')  // убрать русские подсказки
    .replace(/💡|🎤|✓|👋|🙏|😊/g, '')
    .trim()

  if (!clean || typeof window === 'undefined') return

  window.speechSynthesis.cancel() // отменить предыдущее

  const utterance = new SpeechSynthesisUtterance(clean)
  utterance.lang = 'en-US'
  utterance.rate = 0.9   // чуть медленнее для учебы
  utterance.pitch = 1.0

  // Выбрать лучший голос
  const voices = window.speechSynthesis.getVoices()
  const preferredVoice = voices.find(v =>
    v.lang === 'en-US' && (v.name.includes('Google') || v.name.includes('Samantha') || v.name.includes('Daniel'))
  ) || voices.find(v => v.lang.startsWith('en'))

  if (preferredVoice) utterance.voice = preferredVoice

  window.speechSynthesis.speak(utterance)
}
```

Вызывать `speakText(reply)` после получения ответа AI, если `voiceMode`.

**2.6. MediaRecorder для захвата аудио**

```typescript
const mediaRecorderRef = useRef<MediaRecorder | null>(null)
const audioChunksRef = useRef<Blob[]>([])

const startVoiceRecording = async () => {
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true })
    const recorder = new MediaRecorder(stream)
    audioChunksRef.current = []

    recorder.ondataavailable = (e) => {
      if (e.data.size > 0) audioChunksRef.current.push(e.data)
    }

    recorder.onstop = () => {
      const blob = new Blob(audioChunksRef.current, { type: 'audio/webm' })
      const audioUrl = URL.createObjectURL(blob)
      // Сохранить audioUrl для текущего сообщения
      pendingAudioUrlRef.current = audioUrl
      // Остановить треки
      stream.getTracks().forEach(t => t.stop())
    }

    mediaRecorderRef.current = recorder
    recorder.start()
  } catch {
    console.error('Microphone access denied')
  }
}

const stopVoiceRecording = () => {
  mediaRecorderRef.current?.stop()
}
```

**2.7. Обновить `startListening`:**

В voice mode — запускать и SpeechRecognition, и MediaRecorder одновременно:
```typescript
const startListening = async () => {
  if (voiceMode) {
    await startVoiceRecording()
  }
  // ... существующий код SpeechRecognition
}
```

В `sendMessage` — если есть `pendingAudioUrlRef.current`, добавить его в сообщение:
```typescript
const userMsg: Message = {
  id: Date.now().toString(),
  role: 'user',
  content: trimmed,
  timestamp: new Date().toISOString(),
  isVoice: !!pendingAudioUrlRef.current,
  audioUrl: pendingAudioUrlRef.current || undefined,
}
pendingAudioUrlRef.current = null
```

**2.8. UI изменения в хедере ai-tutor:**

Добавить тогл Voice Mode рядом с кнопкой reset:
```tsx
<button
  onClick={() => {
    setVoiceMode(v => !v)
    if (voiceMode) window.speechSynthesis.cancel()
  }}
  className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium transition-all ${
    voiceMode
      ? 'bg-[#6366F1] text-white shadow-lg shadow-indigo-500/30'
      : 'bg-[#1E293B] text-[#94A3B8] hover:text-white'
  }`}
>
  {voiceMode ? '🔊 Voice ON' : '🔇 Voice OFF'}
</button>
```

Когда voice mode включён — кнопка микрофона должна выглядеть активнее (акцент).

---

## ══════════════════════════════════════════════════════════════
## ФИЧА 3: AI ГЕНЕРАЦИЯ КОНТЕНТА ПО ВСЕМУ ПРИЛОЖЕНИЮ
## ══════════════════════════════════════════════════════════════

### 3.1. Создать API endpoint `/api/ai/generate/route.ts`

```typescript
// app/api/ai/generate/route.ts
import { NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'

export async function POST(request: Request) {
  const authHeader = request.headers.get('Authorization')
  const token = authHeader?.replace('Bearer ', '')
  if (!token) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const supabase = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { global: { headers: { Authorization: `Bearer ${token}` } } }
  )
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { type, context, level = 'B1' } = await request.json()

  const prompts: Record<string, string> = {
    vocabulary_example: `Generate 2 natural example sentences for the word "${context}" at CEFR level ${level}. Return JSON: {"sentences": ["...", "..."]}`,
    vocabulary_set: `Generate 8 useful English vocabulary words for topic: "${context}" at level ${level}. For each word include: word, translation (Russian), example sentence. Return JSON: {"words": [{"word":"...","translation":"...","example":"..."}]}`,
    grammar_exercise: `Create a fill-in-the-blank grammar exercise about "${context}" at level ${level}. Return JSON: {"question":"...","blank":"...","answer":"...","options":["...","...","...","..."],"explanation":"..."}`,
    mini_story: `Write a short English story (150-200 words) about "${context}" at level ${level}. Make it engaging and educational. Return JSON: {"title":"...","content":"...","vocabulary":[{"word":"...","meaning":"..."}]}`,
    reading_text: `Write a reading comprehension text (200-250 words) about "${context}" at level ${level}. Include 3 comprehension questions. Return JSON: {"title":"...","text":"...","questions":[{"question":"...","answer":"..."}]}`,
    word_formation: `Create a word formation exercise using the root word "${context}". Generate noun, verb, adjective, adverb forms with examples. Return JSON: {"root":"...","forms":[{"type":"noun/verb/adj/adverb","word":"...","example":"..."}]}`,
    writing_prompt: `Generate an engaging writing prompt about "${context}" for level ${level} English learner. Include helpful vocabulary. Return JSON: {"prompt":"...","tips":["..."],"vocabulary":["..."]}`,
    conversation_starter: `Generate 5 conversation starter questions about "${context}" for English practice at level ${level}. Return JSON: {"starters":["...","...","...","...","..."]}`,
  }

  const systemPrompt = prompts[type]
  if (!systemPrompt) return NextResponse.json({ error: 'Unknown type' }, { status: 400 })

  const response = await fetch('https://api.groq.com/openai/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.GROQ_API_KEY}`,
    },
    body: JSON.stringify({
      model: 'llama-3.1-8b-instant',
      messages: [
        { role: 'system', content: 'You are an English language teacher. Always respond with valid JSON only, no markdown, no explanation.' },
        { role: 'user', content: systemPrompt },
      ],
      max_tokens: 800,
      temperature: 0.8,
    }),
  })

  if (!response.ok) return NextResponse.json({ error: 'AI error' }, { status: 500 })

  const data = await response.json()
  const raw = data.choices?.[0]?.message?.content || '{}'

  try {
    // Извлечь JSON из ответа (на случай если модель добавила markdown)
    const jsonMatch = raw.match(/\{[\s\S]*\}/)
    const result = jsonMatch ? JSON.parse(jsonMatch[0]) : JSON.parse(raw)
    return NextResponse.json(result)
  } catch {
    return NextResponse.json({ raw })
  }
}
```

### 3.2. Создать переиспользуемый компонент `components/ui/AIGenerateButton.tsx`

```tsx
"use client"

import { useState } from "react"
import { Sparkles, Loader2 } from "lucide-react"
import { supabase } from "@/lib/supabase"

interface AIGenerateButtonProps {
  type: string           // тип генерации (vocabulary_set, grammar_exercise, etc.)
  context: string        // контекст (тема, слово, etc.)
  level?: string
  onResult: (data: Record<string, unknown>) => void
  label?: string
  className?: string
  variant?: 'button' | 'icon' | 'inline'
}

export function AIGenerateButton({
  type, context, level = 'B1', onResult, label = 'Сгенерировать AI',
  className = '', variant = 'button'
}: AIGenerateButtonProps) {
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState(false)

  const generate = async () => {
    setLoading(true)
    setError(false)
    try {
      const { data: { session } } = await supabase.auth.getSession()
      const res = await fetch('/api/ai/generate', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${session?.access_token ?? ''}`,
        },
        body: JSON.stringify({ type, context, level }),
      })
      if (!res.ok) throw new Error('Failed')
      const data = await res.json()
      onResult(data)
    } catch {
      setError(true)
      setTimeout(() => setError(false), 2000)
    } finally {
      setLoading(false)
    }
  }

  if (variant === 'icon') {
    return (
      <button
        onClick={generate}
        disabled={loading}
        title={label}
        className={`p-1.5 rounded-lg hover:bg-primary/10 text-primary transition-all disabled:opacity-50 ${className}`}
      >
        {loading ? <Loader2 className="w-4 h-4 animate-spin" /> : <Sparkles className="w-4 h-4" />}
      </button>
    )
  }

  if (variant === 'inline') {
    return (
      <button
        onClick={generate}
        disabled={loading}
        className={`flex items-center gap-1.5 text-sm text-primary hover:text-primary/80 font-medium transition-all disabled:opacity-50 ${className}`}
      >
        {loading ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Sparkles className="w-3.5 h-3.5" />}
        {error ? 'Ошибка, попробуй ещё' : label}
      </button>
    )
  }

  return (
    <button
      onClick={generate}
      disabled={loading}
      className={`flex items-center gap-2 px-4 py-2 bg-primary/10 hover:bg-primary/20 text-primary rounded-xl font-medium text-sm transition-all disabled:opacity-50 border border-primary/20 ${className}`}
    >
      {loading ? <Loader2 className="w-4 h-4 animate-spin" /> : <Sparkles className="w-4 h-4" />}
      {error ? 'Ошибка, попробуй ещё' : (loading ? 'Генерирую...' : label)}
    </button>
  )
}
```

### 3.3. Добавить AI кнопки на страницы

**`app/(dashboard)/vocabulary/page.tsx`** — добавить кнопку генерации примеров для слов:

Найди раздел где отображается слово в режиме изучения (study mode, карточки).
Под словом добавить:
```tsx
import { AIGenerateButton } from "@/components/ui/AIGenerateButton"

// В карточке слова, под определением:
<AIGenerateButton
  type="vocabulary_example"
  context={currentWord.word}
  level={userLevel}
  variant="inline"
  label="AI примеры"
  onResult={(data) => {
    // Показать примеры в модалке или под карточкой
    setAIExamples((data as { sentences?: string[] }).sentences || [])
  }}
/>
{aiExamples.length > 0 && (
  <div className="mt-3 space-y-1">
    {aiExamples.map((ex, i) => (
      <p key={i} className="text-sm text-muted-foreground italic">"{ex}"</p>
    ))}
  </div>
)}
```

Добавить состояние: `const [aiExamples, setAIExamples] = useState<string[]>([])`

**`app/(dashboard)/mini-stories/page.tsx`** — кнопка "Сгенерировать историю":

Найди блок где нет историй или добавь кнопку в хедер страницы:
```tsx
import { AIGenerateButton } from "@/components/ui/AIGenerateButton"

// В секции заголовка страницы или рядом с фильтрами:
const [aiStory, setAIStory] = useState<{title?: string, content?: string, vocabulary?: {word: string, meaning: string}[]} | null>(null)

<AIGenerateButton
  type="mini_story"
  context={`level ${level} general interest topic`}
  level={level}
  label="✨ Сгенерировать историю"
  onResult={(data) => setAIStory(data as typeof aiStory)}
/>

// Показать AI историю под списком:
{aiStory && (
  <div className="mt-6 p-6 rounded-2xl border border-primary/20 bg-primary/5">
    <div className="flex items-center gap-2 mb-3">
      <span className="text-primary">✨</span>
      <h3 className="font-semibold">{aiStory.title}</h3>
      <span className="text-xs text-muted-foreground ml-auto">AI Generated</span>
    </div>
    <p className="text-sm leading-relaxed">{aiStory.content}</p>
    {aiStory.vocabulary && aiStory.vocabulary.length > 0 && (
      <div className="mt-4 flex flex-wrap gap-2">
        {aiStory.vocabulary.map((v, i) => (
          <span key={i} className="px-2 py-1 bg-primary/10 text-primary rounded-lg text-xs">
            <b>{v.word}</b> — {v.meaning}
          </span>
        ))}
      </div>
    )}
    <button onClick={() => setAIStory(null)} className="mt-3 text-xs text-muted-foreground hover:text-foreground">
      Закрыть
    </button>
  </div>
)}
```

**`app/(dashboard)/grammar-exercises/page.tsx`** — кнопка "Новое задание AI":

Найди хедер страницы и добавь рядом с фильтром:
```tsx
import { AIGenerateButton } from "@/components/ui/AIGenerateButton"

const [aiExercise, setAIExercise] = useState<{question?: string, answer?: string, options?: string[], explanation?: string} | null>(null)

<AIGenerateButton
  type="grammar_exercise"
  context={selectedTopic || 'mixed grammar'}
  level={userLevel}
  label="AI задание"
  variant="inline"
  onResult={(data) => setAIExercise(data as typeof aiExercise)}
/>

// Показать AI упражнение:
{aiExercise && aiExercise.question && (
  <div className="p-5 rounded-2xl border border-primary/20 bg-card mb-4">
    <div className="flex items-center gap-2 mb-3">
      <span className="text-xs font-medium text-primary bg-primary/10 px-2 py-0.5 rounded-full">✨ AI упражнение</span>
    </div>
    <p className="font-medium mb-3">{aiExercise.question}</p>
    <div className="grid grid-cols-2 gap-2">
      {aiExercise.options?.map((opt, i) => (
        <button
          key={i}
          onClick={() => {
            if (opt === aiExercise.answer) {
              alert('✅ Правильно! ' + (aiExercise.explanation || ''))
            } else {
              alert('❌ Неверно. Правильный ответ: ' + aiExercise.answer)
            }
          }}
          className="px-3 py-2 rounded-xl border border-border hover:border-primary text-sm text-left transition-all"
        >
          {opt}
        </button>
      ))}
    </div>
    <button onClick={() => setAIExercise(null)} className="mt-3 text-xs text-muted-foreground hover:text-foreground">
      Закрыть
    </button>
  </div>
)}
```

**`app/(dashboard)/word-formation/page.tsx`** — кнопка "AI словообразование":

Найди хедер или панель с фильтрами:
```tsx
import { AIGenerateButton } from "@/components/ui/AIGenerateButton"

const [aiWordForms, setAIWordForms] = useState<{root?: string, forms?: {type: string, word: string, example: string}[]} | null>(null)

<AIGenerateButton
  type="word_formation"
  context={currentWord || 'nature'}
  level={userLevel}
  label="AI словообразование"
  variant="inline"
  onResult={(data) => setAIWordForms(data as typeof aiWordForms)}
/>

{aiWordForms && aiWordForms.forms && (
  <div className="p-4 rounded-2xl border border-primary/20 bg-primary/5 mt-4">
    <p className="text-xs text-primary font-medium mb-3">✨ Словообразование от "{aiWordForms.root}"</p>
    <div className="grid grid-cols-2 gap-2">
      {aiWordForms.forms.map((f, i) => (
        <div key={i} className="p-2 bg-card rounded-xl border border-border">
          <span className="text-xs text-muted-foreground capitalize">{f.type}</span>
          <p className="font-semibold text-sm">{f.word}</p>
          <p className="text-xs text-muted-foreground italic">{f.example}</p>
        </div>
      ))}
    </div>
    <button onClick={() => setAIWordForms(null)} className="mt-2 text-xs text-muted-foreground">Закрыть</button>
  </div>
)}
```

**`app/(dashboard)/lessons/page.tsx`** — кнопка AI для генерации темы урока (добавить в хедер страницы):

Найди где-то вверху список уроков, добавь кнопку:
```tsx
import { AIGenerateButton } from "@/components/ui/AIGenerateButton"

<AIGenerateButton
  type="conversation_starter"
  context="English lesson topics"
  level={userLevel}
  label="AI темы для практики"
  variant="inline"
  onResult={(data) => {
    const starters = (data as { starters?: string[] }).starters || []
    setAIConversationStarters(starters)
  }}
/>
```

---

## ══════════════════════════════════════════════════════════════
## ФИНАЛЬНЫЕ ШАГИ
## ══════════════════════════════════════════════════════════════

### Шаг 1: Проверить TypeScript
```bash
npx tsc --noEmit 2>&1 | head -30
```
Исправить все ошибки.

### Шаг 2: Проверить что нет broken imports
```bash
grep -r "from '@/components/ui/AIGenerateButton'" app/ components/ | head -10
```

### Шаг 3: Убедиться что все нужные состояния добавлены
Для каждой страницы где добавлены AI кнопки — проверить что useState хуки добавлены в начале компонента.

### Шаг 4: Проверить что onboarding wizard не показывается для существующих пользователей
В Supabase новые пользователи получат `onboarding_completed = null` (не true) → wizard покажется.
Существующие пользователи у которых `onboarding_completed = null` → тоже увидят wizard (это нормально для первого запуска фичи).

### Шаг 5: Git commit
```bash
cd /path/to/fluenta
git add -A
git commit -m "feat: onboarding wizard, voice AI responses, AI generation buttons throughout app"
```

---

## ВАЖНЫЕ ЗАМЕТКИ

1. **Не трогай** существующую логику SpeechRecognition — только расширяй её
2. **Для MediaRecorder** — браузер запросит разрешение на микрофон, это нормально
3. **Web Speech Synthesis** работает в Chrome/Edge/Safari — в Firefox может не работать, это ок
4. **Onboarding** — файл миграции SQL только для документации, реально применять не нужно (можно через Supabase dashboard). В коде добавь fallback: если `onboarding_completed` колонки нет в ответе — не показывать wizard (проверяй `'onboarding_completed' in profile`)
5. **AI generate API** — используй GROQ_API_KEY (уже есть в .env.local)
6. Если какой-то файл слишком сложно изменить без риска сломать — добавь AI кнопку в более простое место на той же странице
